# GEH Lab 보안 점검 · 2026-09-16 r4

검토한 소스 범위에서 의도적인 백도어나 관리자 권한 우회 경로는 발견하지 못했습니다. 소스 검토와 로컬 재현 결과이며, 운영 계정·배포 설정·Firebase 규칙 전체가 안전하다는 보증은 아닙니다. 운영 데이터 쓰기·삭제, 공격 요청은 수행하지 않았습니다.

## 수정한 문제

### 삭제한 본문이 공개 문서에 남음

기존 휴지통 이동과 영구 삭제는 `deleted: true`를 기존 공개 문서에 병합했습니다. 화면에서는 숨겨졌지만, 원래 이름·연락처·본문·사진 필드는 문서에 남았습니다. 저장소의 Firestore 규칙은 해당 공개 컬렉션의 읽기를 허용하므로 원문을 직접 읽을 수 있는 구조였습니다.

`saveDocument`에 명시적인 교체 옵션을 추가하고, 휴지통 이동·영구 삭제에서 `{ merge: false }`로 삭제 표식만 남기도록 수정했습니다. 복원용 원본은 관리자 전용 `trash`에 저장합니다. 5개 컬렉션의 원문 제거, 복원, 기존 문서 영구 삭제, 실패 시 보존 등 12개 오프라인 검증을 통과했습니다.

**기존 운영 데이터는 자동 정리하지 않았습니다.** 과거 삭제 문서에 이미 남은 본문은 별도 확인과 정리가 필요합니다. 캐시된 과거 응답이나 공개 이미지 URL을 이미 받은 사람의 복사본까지 회수하는 기능은 아닙니다.

### 빌드 의존성 취약점

npm 공식 레지스트리의 잠금 파일 검사에서 개발 도구의 간접 의존성 2개가 보고되었습니다. 기존 호환 범위 안에서 다음과 같이 갱신했습니다.

| 의존성 | 이전 | 수정 |
|---|---|---|
| PostCSS | 8.5.17 | 8.5.28 |
| nanoid | 3.3.16 | 3.3.19 |

재검사 결과 알려진 취약점은 0건입니다. 공개 사이트에서 실제 악용되었다는 증거를 확인한 것은 아닙니다. 근거: [PostCSS 권고](https://github.com/advisories/GHSA-fxqj-rqcc-2cmp), [nanoid 권고](https://github.com/advisories/GHSA-2v37-7h3g-55p8).

## 확인한 경계

- 로그인은 기존 Google/Firebase 인증을 사용합니다. 코드상의 Firestore·Storage 쓰기와 휴지통 읽기는 관리자 규칙으로 제한됩니다. 브라우저 관리자 목록은 서버 권한의 대체물이 아닙니다.
- 로컬 관리자 미리보기는 loopback 환경과 로컬 저장소에 한정됩니다. 이 설정으로 운영 Firestore 규칙을 우회하는 경로는 찾지 못했습니다.
- 서버 페이지는 페이지·언어·공개 컬렉션을 허용 목록으로 검사합니다. Firestore 요청 호스트는 고정되어 있으며 임의 URL 요청이나 임의 파일 읽기 경로는 찾지 못했습니다.
- 제목·본문의 HTML 이스케이프와 인라인 JSON의 `</script>` 차단을 확인했습니다. 설정 코드는 실행하지 않고 공개 설정 리터럴만 읽습니다.
- 검사 대상에서 개인 키, 서비스 계정 키, 장기 비밀 토큰, `eval`·셸 실행 코드는 발견하지 못했습니다. Firebase 웹 설정은 클라이언트용 공개 설정이며 서비스 계정 비밀 키와 다릅니다.
- 외부 실행 코드는 Firebase SDK, 연락처의 Daum 지도, Vercel 분석 스크립트로 확인했습니다. 문서·논문 링크와 이 출처 외에 의심스러운 전송 경로를 확인하지 못했습니다.

## 남은 확인 사항

- 운영 Firebase에 실제 적용된 규칙과 인증 제공자 설정은 콘솔에서 대조하지 못했습니다. 저장소 규칙이 배포되었다고 가정하지 않습니다. GitHub/Vercel 업로드가 Firebase 규칙을 배포하지는 않습니다.
- 관리자 이메일 규칙에 `email_verified` 조건은 없습니다. 현재 Google 로그인 경로에서 우회는 재현되지 않았습니다. 인증 제공자 변경 시 검증된 이메일·UID 기반 허용 목록을 함께 검토할 필요가 있습니다.
- 일부 게시판·논문 URL은 허용 프로토콜 검사가 일관되지 않습니다. HTML 이스케이프와 `target="_blank" rel="noreferrer"`는 있지만, HTTP/HTTPS 제한을 통일하는 후속 보완을 권장합니다. 실행 가능한 XSS로 확인한 항목은 아닙니다.
- 서버 캐시와 시간 제한은 읽기 요청을 줄이지만 공개 API의 호출 남용을 완전히 막지 않습니다. 할당량·운영 요청 추세는 별도 모니터링 대상입니다.
- Git 전체 이력, 운영 로그, 접근 권한, 과거 침해 여부에 대한 전면 조사는 포함하지 않았습니다.

## 검증

`npm run verify` 전체 통과. 브라우저에서 국문·영문 공개 페이지 콘텐츠와 홈 논문 팝업을 확인했습니다. Vercel 파일 추적 결과의 418개 파일과 명시적 템플릿만 복사한 격리 환경에서도 12개 공개 경로가 정상 응답했습니다. 이 검증은 실제 Vercel 운영 재배포 확인과는 구분됩니다.
